{
    "configurations": [
        {
            "name": "Debug Server",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/api/",
            "envFile": "${workspaceFolder}/.env",
            "dlvFlags": ["--log"]
        }
    ]
}