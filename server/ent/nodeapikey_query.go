// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/nullsploit01/iosync/ent/node"
	"github.com/nullsploit01/iosync/ent/nodeapikey"
	"github.com/nullsploit01/iosync/ent/nodevalues"
	"github.com/nullsploit01/iosync/ent/predicate"
)

// NodeApiKeyQuery is the builder for querying NodeApiKey entities.
type NodeApiKeyQuery struct {
	config
	ctx        *QueryContext
	order      []nodeapikey.OrderOption
	inters     []Interceptor
	predicates []predicate.NodeApiKey
	withNode   *NodeQuery
	withValues *NodeValuesQuery
	withFKs    bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the NodeApiKeyQuery builder.
func (nakq *NodeApiKeyQuery) Where(ps ...predicate.NodeApiKey) *NodeApiKeyQuery {
	nakq.predicates = append(nakq.predicates, ps...)
	return nakq
}

// Limit the number of records to be returned by this query.
func (nakq *NodeApiKeyQuery) Limit(limit int) *NodeApiKeyQuery {
	nakq.ctx.Limit = &limit
	return nakq
}

// Offset to start from.
func (nakq *NodeApiKeyQuery) Offset(offset int) *NodeApiKeyQuery {
	nakq.ctx.Offset = &offset
	return nakq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (nakq *NodeApiKeyQuery) Unique(unique bool) *NodeApiKeyQuery {
	nakq.ctx.Unique = &unique
	return nakq
}

// Order specifies how the records should be ordered.
func (nakq *NodeApiKeyQuery) Order(o ...nodeapikey.OrderOption) *NodeApiKeyQuery {
	nakq.order = append(nakq.order, o...)
	return nakq
}

// QueryNode chains the current query on the "node" edge.
func (nakq *NodeApiKeyQuery) QueryNode() *NodeQuery {
	query := (&NodeClient{config: nakq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := nakq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := nakq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nodeapikey.Table, nodeapikey.FieldID, selector),
			sqlgraph.To(node.Table, node.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, nodeapikey.NodeTable, nodeapikey.NodeColumn),
		)
		fromU = sqlgraph.SetNeighbors(nakq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryValues chains the current query on the "values" edge.
func (nakq *NodeApiKeyQuery) QueryValues() *NodeValuesQuery {
	query := (&NodeValuesClient{config: nakq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := nakq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := nakq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(nodeapikey.Table, nodeapikey.FieldID, selector),
			sqlgraph.To(nodevalues.Table, nodevalues.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, nodeapikey.ValuesTable, nodeapikey.ValuesColumn),
		)
		fromU = sqlgraph.SetNeighbors(nakq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first NodeApiKey entity from the query.
// Returns a *NotFoundError when no NodeApiKey was found.
func (nakq *NodeApiKeyQuery) First(ctx context.Context) (*NodeApiKey, error) {
	nodes, err := nakq.Limit(1).All(setContextOp(ctx, nakq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{nodeapikey.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) FirstX(ctx context.Context) *NodeApiKey {
	node, err := nakq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first NodeApiKey ID from the query.
// Returns a *NotFoundError when no NodeApiKey ID was found.
func (nakq *NodeApiKeyQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nakq.Limit(1).IDs(setContextOp(ctx, nakq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{nodeapikey.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) FirstIDX(ctx context.Context) int {
	id, err := nakq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single NodeApiKey entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one NodeApiKey entity is found.
// Returns a *NotFoundError when no NodeApiKey entities are found.
func (nakq *NodeApiKeyQuery) Only(ctx context.Context) (*NodeApiKey, error) {
	nodes, err := nakq.Limit(2).All(setContextOp(ctx, nakq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{nodeapikey.Label}
	default:
		return nil, &NotSingularError{nodeapikey.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) OnlyX(ctx context.Context) *NodeApiKey {
	node, err := nakq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only NodeApiKey ID in the query.
// Returns a *NotSingularError when more than one NodeApiKey ID is found.
// Returns a *NotFoundError when no entities are found.
func (nakq *NodeApiKeyQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = nakq.Limit(2).IDs(setContextOp(ctx, nakq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{nodeapikey.Label}
	default:
		err = &NotSingularError{nodeapikey.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) OnlyIDX(ctx context.Context) int {
	id, err := nakq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of NodeApiKeys.
func (nakq *NodeApiKeyQuery) All(ctx context.Context) ([]*NodeApiKey, error) {
	ctx = setContextOp(ctx, nakq.ctx, ent.OpQueryAll)
	if err := nakq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*NodeApiKey, *NodeApiKeyQuery]()
	return withInterceptors[[]*NodeApiKey](ctx, nakq, qr, nakq.inters)
}

// AllX is like All, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) AllX(ctx context.Context) []*NodeApiKey {
	nodes, err := nakq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of NodeApiKey IDs.
func (nakq *NodeApiKeyQuery) IDs(ctx context.Context) (ids []int, err error) {
	if nakq.ctx.Unique == nil && nakq.path != nil {
		nakq.Unique(true)
	}
	ctx = setContextOp(ctx, nakq.ctx, ent.OpQueryIDs)
	if err = nakq.Select(nodeapikey.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) IDsX(ctx context.Context) []int {
	ids, err := nakq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (nakq *NodeApiKeyQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, nakq.ctx, ent.OpQueryCount)
	if err := nakq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, nakq, querierCount[*NodeApiKeyQuery](), nakq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) CountX(ctx context.Context) int {
	count, err := nakq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (nakq *NodeApiKeyQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, nakq.ctx, ent.OpQueryExist)
	switch _, err := nakq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (nakq *NodeApiKeyQuery) ExistX(ctx context.Context) bool {
	exist, err := nakq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the NodeApiKeyQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (nakq *NodeApiKeyQuery) Clone() *NodeApiKeyQuery {
	if nakq == nil {
		return nil
	}
	return &NodeApiKeyQuery{
		config:     nakq.config,
		ctx:        nakq.ctx.Clone(),
		order:      append([]nodeapikey.OrderOption{}, nakq.order...),
		inters:     append([]Interceptor{}, nakq.inters...),
		predicates: append([]predicate.NodeApiKey{}, nakq.predicates...),
		withNode:   nakq.withNode.Clone(),
		withValues: nakq.withValues.Clone(),
		// clone intermediate query.
		sql:  nakq.sql.Clone(),
		path: nakq.path,
	}
}

// WithNode tells the query-builder to eager-load the nodes that are connected to
// the "node" edge. The optional arguments are used to configure the query builder of the edge.
func (nakq *NodeApiKeyQuery) WithNode(opts ...func(*NodeQuery)) *NodeApiKeyQuery {
	query := (&NodeClient{config: nakq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	nakq.withNode = query
	return nakq
}

// WithValues tells the query-builder to eager-load the nodes that are connected to
// the "values" edge. The optional arguments are used to configure the query builder of the edge.
func (nakq *NodeApiKeyQuery) WithValues(opts ...func(*NodeValuesQuery)) *NodeApiKeyQuery {
	query := (&NodeValuesClient{config: nakq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	nakq.withValues = query
	return nakq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		APIKey string `json:"api_key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.NodeApiKey.Query().
//		GroupBy(nodeapikey.FieldAPIKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (nakq *NodeApiKeyQuery) GroupBy(field string, fields ...string) *NodeApiKeyGroupBy {
	nakq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &NodeApiKeyGroupBy{build: nakq}
	grbuild.flds = &nakq.ctx.Fields
	grbuild.label = nodeapikey.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		APIKey string `json:"api_key,omitempty"`
//	}
//
//	client.NodeApiKey.Query().
//		Select(nodeapikey.FieldAPIKey).
//		Scan(ctx, &v)
func (nakq *NodeApiKeyQuery) Select(fields ...string) *NodeApiKeySelect {
	nakq.ctx.Fields = append(nakq.ctx.Fields, fields...)
	sbuild := &NodeApiKeySelect{NodeApiKeyQuery: nakq}
	sbuild.label = nodeapikey.Label
	sbuild.flds, sbuild.scan = &nakq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a NodeApiKeySelect configured with the given aggregations.
func (nakq *NodeApiKeyQuery) Aggregate(fns ...AggregateFunc) *NodeApiKeySelect {
	return nakq.Select().Aggregate(fns...)
}

func (nakq *NodeApiKeyQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range nakq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, nakq); err != nil {
				return err
			}
		}
	}
	for _, f := range nakq.ctx.Fields {
		if !nodeapikey.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if nakq.path != nil {
		prev, err := nakq.path(ctx)
		if err != nil {
			return err
		}
		nakq.sql = prev
	}
	return nil
}

func (nakq *NodeApiKeyQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*NodeApiKey, error) {
	var (
		nodes       = []*NodeApiKey{}
		withFKs     = nakq.withFKs
		_spec       = nakq.querySpec()
		loadedTypes = [2]bool{
			nakq.withNode != nil,
			nakq.withValues != nil,
		}
	)
	if nakq.withNode != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, nodeapikey.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*NodeApiKey).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &NodeApiKey{config: nakq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, nakq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := nakq.withNode; query != nil {
		if err := nakq.loadNode(ctx, query, nodes, nil,
			func(n *NodeApiKey, e *Node) { n.Edges.Node = e }); err != nil {
			return nil, err
		}
	}
	if query := nakq.withValues; query != nil {
		if err := nakq.loadValues(ctx, query, nodes,
			func(n *NodeApiKey) { n.Edges.Values = []*NodeValues{} },
			func(n *NodeApiKey, e *NodeValues) { n.Edges.Values = append(n.Edges.Values, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (nakq *NodeApiKeyQuery) loadNode(ctx context.Context, query *NodeQuery, nodes []*NodeApiKey, init func(*NodeApiKey), assign func(*NodeApiKey, *Node)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*NodeApiKey)
	for i := range nodes {
		if nodes[i].node_api_keys == nil {
			continue
		}
		fk := *nodes[i].node_api_keys
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(node.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "node_api_keys" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (nakq *NodeApiKeyQuery) loadValues(ctx context.Context, query *NodeValuesQuery, nodes []*NodeApiKey, init func(*NodeApiKey), assign func(*NodeApiKey, *NodeValues)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*NodeApiKey)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.NodeValues(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(nodeapikey.ValuesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.node_api_key_values
		if fk == nil {
			return fmt.Errorf(`foreign-key "node_api_key_values" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "node_api_key_values" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (nakq *NodeApiKeyQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := nakq.querySpec()
	_spec.Node.Columns = nakq.ctx.Fields
	if len(nakq.ctx.Fields) > 0 {
		_spec.Unique = nakq.ctx.Unique != nil && *nakq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, nakq.driver, _spec)
}

func (nakq *NodeApiKeyQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(nodeapikey.Table, nodeapikey.Columns, sqlgraph.NewFieldSpec(nodeapikey.FieldID, field.TypeInt))
	_spec.From = nakq.sql
	if unique := nakq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if nakq.path != nil {
		_spec.Unique = true
	}
	if fields := nakq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, nodeapikey.FieldID)
		for i := range fields {
			if fields[i] != nodeapikey.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := nakq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := nakq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := nakq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := nakq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (nakq *NodeApiKeyQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(nakq.driver.Dialect())
	t1 := builder.Table(nodeapikey.Table)
	columns := nakq.ctx.Fields
	if len(columns) == 0 {
		columns = nodeapikey.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if nakq.sql != nil {
		selector = nakq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if nakq.ctx.Unique != nil && *nakq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range nakq.predicates {
		p(selector)
	}
	for _, p := range nakq.order {
		p(selector)
	}
	if offset := nakq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := nakq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// NodeApiKeyGroupBy is the group-by builder for NodeApiKey entities.
type NodeApiKeyGroupBy struct {
	selector
	build *NodeApiKeyQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (nakgb *NodeApiKeyGroupBy) Aggregate(fns ...AggregateFunc) *NodeApiKeyGroupBy {
	nakgb.fns = append(nakgb.fns, fns...)
	return nakgb
}

// Scan applies the selector query and scans the result into the given value.
func (nakgb *NodeApiKeyGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, nakgb.build.ctx, ent.OpQueryGroupBy)
	if err := nakgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NodeApiKeyQuery, *NodeApiKeyGroupBy](ctx, nakgb.build, nakgb, nakgb.build.inters, v)
}

func (nakgb *NodeApiKeyGroupBy) sqlScan(ctx context.Context, root *NodeApiKeyQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(nakgb.fns))
	for _, fn := range nakgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*nakgb.flds)+len(nakgb.fns))
		for _, f := range *nakgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*nakgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := nakgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// NodeApiKeySelect is the builder for selecting fields of NodeApiKey entities.
type NodeApiKeySelect struct {
	*NodeApiKeyQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (naks *NodeApiKeySelect) Aggregate(fns ...AggregateFunc) *NodeApiKeySelect {
	naks.fns = append(naks.fns, fns...)
	return naks
}

// Scan applies the selector query and scans the result into the given value.
func (naks *NodeApiKeySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, naks.ctx, ent.OpQuerySelect)
	if err := naks.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*NodeApiKeyQuery, *NodeApiKeySelect](ctx, naks.NodeApiKeyQuery, naks, naks.inters, v)
}

func (naks *NodeApiKeySelect) sqlScan(ctx context.Context, root *NodeApiKeyQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(naks.fns))
	for _, fn := range naks.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*naks.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := naks.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
